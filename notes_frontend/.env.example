# Toggle mock API (localStorage). 'true' by default; set to 'false' to use real backend.
REACT_APP_USE_MOCK=true

# Optional: base URL for real backend API (only used when REACT_APP_USE_MOCK=false)
REACT_APP_API_BASE_URL=/api

# Optional: failure rate for mock API (0.0 to 1.0)
REACT_APP_MOCK_FAILURE_RATE=0
